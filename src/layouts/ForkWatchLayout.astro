---
import type { Locale } from "@/i18n/locales";
import Layout from "./Layout.astro";

type Props = {
  description: string;
  locale: Locale;
  title: string;
};

const { description, locale, title } = Astro.props;
---

<Layout description={description} locale={locale} title={title}>
  <slot />
</Layout>
